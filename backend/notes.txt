We should probably split the server code into class depending on how complex it gets.

Will probably run backend on a server so front end
work for everyone by default. - Gabriel


Commands for python

 -run 

 python -m venv venv
 //then enter then venv usually . venv/Scripts/activate but could be something else
